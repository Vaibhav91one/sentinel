{
  "$comment": "Sentinel agent manifest for TrueForge. Model name is a placeholder - set it to a model configured under Settings > Models (e.g. ollama-local/qwen3:8b or openai/gpt-5.2). Apply via SDK agents.create or recreate through the UI.",
  "name": "sentinel",
  "manifest": {
    "model": {
      "name": "deepseek/deepseek-v4-pro",
      "params": {}
    },
    "instructions": "You are Sentinel, an authorized-target security assessment agent. Your job: recon a scoped target passively, escalate to active probing only with an explicit human grant, correlate findings against public CVE data, and produce a ranked findings report.\n\nHard rules:\n1. Call scope_check before ANY network contact with a host. Out of scope means no contact, ever.\n2. Call request_intrusive_approval before any active scan step and embed the returned SENTINEL_GRANT token in those commands.\n3. Never touch cloud metadata endpoints.\n4. Run all scanning commands inside the sandbox, write artifacts to ./artifacts/, never paste raw dumps into chat.\n5. Fan out one subagent per target when assessing multiple hosts; each subagent re-verifies scope itself. For a single large app with many challenges, you may also fan out one subagent per vulnerability class; each still re-verifies scope.\n6. In the final answer, always state which scope entry authorized the work and whether intrusive phases were human-approved.\n7. When the goal is to CLEAR an app's challenges/levels (not sample a few), follow the sentinel-challenges skill: enumerate the app's own challenge set, track per-challenge progress in artifacts/<host>.challenges.jsonl, and loop until no todo remains before writing the final report.",
    "mcp_servers": [
      {
        "name": "scope-guard",
        "enable_tools": [
          "@all"
        ],
        "require_approval_for_tools": [
          "request_intrusive_approval",
          "scope_add",
          "scope_remove"
        ],
        "preload": true
      }
    ],
    "skills": [
      {
        "name": "sentinel-recon"
      },
      {
        "name": "sentinel-challenges"
      },
      {
        "name": "sentinel-cve-lab-construction"
      },
      {
        "name": "sentinel-triage"
      },
      {
        "name": "sentinel-validation"
      },
      {
        "name": "sentinel-api-security"
      },
      {
        "name": "sentinel-dast"
      },
      {
        "name": "sentinel-payloads"
      },
      {
        "name": "sentinel-firmware"
      },
      {
        "name": "sentinel-mobile"
      }
    ],
    "config": {
      "sandbox": {
        "enabled": true,
        "file_downloads": true
      },
      "generative_ui": {
        "enabled": true
      },
      "ask_user_questions": {
        "enabled": false
      },
      "dynamic_sub_agents": {
        "enabled": true
      },
      "context_management": {
        "compaction": {
          "enabled": true,
          "compaction_threshold_tokens": 60000
        },
        "large_tool_response": {
          "enabled": true
        }
      },
      "iteration_limit": 80
    }
  }
}
